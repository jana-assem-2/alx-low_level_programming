#include "main.h"

/**
* _print_rev - print reversed string, followed by a new line
* @s: pointer to the string to point
* Return: void
*/

void _print_rev(char *s)
{
	int i = 0;

	while (*str != '\0')
	{
		i++;
	}
	while (i--)
	{
		_putchar(str[i]);
	}
}
